[Reference](https://reactnative.dev/docs/build-speed)

> We suggest to use `ccache` to cache the compilation of your native builds. 

> You might want to consider using a distributed cache (`sccache`) for your native builds. This could be specifically useful in bigger organizations that are doing frequent native builds.