#### Numbers

Data types - `int`, `float`, `Decimal`, `Fraction`

Division always returns a float.

`//` returns the dividened (i.e. non float part). <br>
`%` returns the remainder. <br>
`**` calculates power (i.e. exponents). <br>

If a variable is not defined (assigned a value), trying to use it gives an error.

Python also has built-in support for `complex numbers`, and uses the `j` or `J` suffix to indicate the imaginary part (e.g. 3+5j).

#### Strings

##### Special characters in Strings

`\` can be used to escape quotes.

`print()` function always prints a cleaner string without the escape.

If characters prefaced by `\` need not  be interpreted as special characters, raw strings can be sued by adding an `r` before the first quote.
```
>>> print('C:\some\name')  # here \n means newline!
C:\some
ame
>>> print(r'C:\some\name')  # note the r before the quote
C:\some\name
```

String literals can span multiple lines. One way is using triple-quotes - `"""..."""` or `'''...'''`. End of lines are automatically included in the string, but it’s possible to prevent this by adding a `\` at the end of the line.

##### Accessing using index, Slicing

Strings can be indexed (subscripted), with the first character having index 0. There is no separate character type; a character is simply a string of size one.

Indices may also be negative numbers, to start counting from the right.

Slicing is also supported. While indexing is used to obtain individual characters, slicing allows you to obtain substring:

```
>>> word[0:2]  # characters from position 0 (included) to 2 (excluded)
'Py'
```
Slice indices have useful defaults; an omitted first index defaults to zero, an omitted second index defaults to the size of the string being sliced.

Attempting to use an index that is too large results in an error.
However, out of range slice indexes are handled gracefully when used for slicing.
```
>>>
>>> word[4:42]
'on'
```

##### Misc.

Two or more string literals (i.e. the ones enclosed between quotes) next to each other are automatically concatenated.
```
>>> 'Py' 'thon'
'Python'
```
This only works with two literals though, not with variables or expressions.

Python strings cannot be changed — they are immutable. Therefore, assigning to an indexed position in the string results in an error.

The built-in function len() returns the length of a string.

##### Formatted String Literals

While generating an output, for variables' values to be inserted in a string, the string needs to be prefixed with `f` or `F`. This is known as `formatted string literal (f-strings)`.
```
print(f'{a}')  # Prints the value of a
print('{a}')   # Just prints {a}
```

The thing between `{}` is likely what is termed as the expression. Additional specifiers can be optionally added to it.
```
print(f'{a:-9}')          # Prefixes a tab to a
print(f'{a:-2}')          # Gets the field to be minimum 2 chraracters wide
print(f'{math.pi:.3f}')   # Rounds pi to 3 decimal places
```

Reference of all specifiers - [Link](https://docs.python.org/3/library/string.html#formatspec)

##### str.format method

Another way of printing string. The expressions to be specified are actually given in the format() method.

```
print('Hi {:-9}'.format(a))   # Here a is prefixed by a tab by virtue of {:-9}
```

```
for x in range(1, 4):
    print('{0:2d} {1:3d} {2:4d}'.format(x, x*x, x*x*x))

Prints this -
1   1    1
2   4    8
3   9   27
```

##### Misc. string functions

`repr()` or `str()` functions return string representations of any type, not just string. Often they are the same.

`str.rjust()` - Right-justifies a string in a field of a given width by padding it with spaces on the left. <br>
`str.ljust()` <br>
`str.center()` <br>
`str.zfill()` - Pads a numeric string on left with zeros.<br>

.split()[0] - Gets the first word. <br>
removeprefix("xyz") - Removes a given prefix.<br>

#### Lists

Its one of Python's compound data types.
```
squares = [1, 4, 9, 16, 25]
```

They too can be indexed and sliced. All slice operations return a new list containing the requested elements.
Unlike strings, which are immutable, lists are a mutable type.

`append()`, `len()`

Assignment to slices is also possible, and this can even change the size of the list or clear it entirely.
```
letters[2:5] = ['C', 'D', 'E']
```

If a collection needs to be modified during iteration, the recommended way is to iterate over a copy of collection or create a new collection and populate it as the original collection is iterated.
```
# Create a sample collection
users = {'Hans': 'active', 'Éléonore': 'inactive', '景太郎': 'active'}

# Strategy:  Iterate over a copy
for user, status in users.copy().items():
    if status == 'inactive':
        del users[user]

# Strategy:  Create a new collection
active_users = {}
for user, status in users.items():
    if status == 'active':
        active_users[user] = status
```

`in` tests whether or not a sequence contains a certain value.
```
if ok in ('n', 'no', 'nop', 'nope'):
```
