All Java code must be inside a class. Its a convention to have file name same as class name.

Method signature - method name + arguments

Method header - signature + modifiers (public, static, etc.) + return type + exception throws list

`Constructor` returns an instance of the class it's in. There is no need to have an explicit return in it.
`this.` new should be used while calling a constructor.
