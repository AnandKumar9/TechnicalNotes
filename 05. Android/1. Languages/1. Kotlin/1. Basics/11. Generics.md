#### Generics

```
class MyList<T> {
    fun get(pos: Int): T {
        TODO("implement")
    }
    fun addItem(item: T) {}
}
```

#### in and out types

An `in` type is a type that can only be passed into a class, not returned. An `out` type is a type that can only be returned from a class.

The `in` and `out` types are directives for Kotlin's type system. The compiler flags types that are not marked in and out appropriately, so you need to know about them.

val and var are about the VALUES of variables.
in and out are about the TYPES of variables.

```
class Aquarium<out T: WaterSupply>(val waterSupply: T) {
    ...
}

interface Cleaner<in T: WaterSupply> { }
```
> Need to try these.

#### Generic functions -

```
fun <T: WaterSupply> isWaterClean(aquarium: Aquarium<T>) { ... }
```
