The basic function syntax.
```
fun printHello() {
    println("Hello World")
}

printHello()

fun main(args: Array<String>) { }
```

When a function doesn't return anything, the return type is actually `kotlin.Unit`.

##### Default value for arguments

Default value can be specified for arguments
```
fun swim(speed: String = "fast") {}

swim()   // uses default speed
swim("slow")   // positional argument
swim(speed="turtle-like")   // named parameter
```

##### Compact functions

`Compact functions` are also called `single-expression functions`. When a function returns the results of a single expression, you can specify the body of the function after an `=` symbol, omit the curly braces `{}`, and omit the `return`.

```
fun isTooHot(temperature: Int) = temperature > 30
```

##### Lambdas and Higher-order functions

A `lambda` is an expression that makes a function. What makes this useful is that the lambda expression can now be passed as data.

```
val waterFilter = { dirty : Int -> dirty / 2 }
println(waterFilter(20))

val waterFilter: (Int) -> Int = { dirty -> dirty / 2 }
```

A `higher-order function` is one which has a lambda passed to it. (The real power of lambdas is using them to create higher-order functions, where the argument to one function is another function.)

```
fun updateDirty(dirty: Int, operation: (Int) -> Int): Int {
   return operation(dirty)
}
```

Kotlin prefers that any parameter that takes a function is the last parameter.

When working with higher-order functions, Kotlin has a special syntax, called the `last parameter call syntax`, which lets you make the code even more concise. In this case, you can pass a lambda for the function parameter, but you don't need to put the lambda inside the parentheses.

```
var dirtyLevel = 19;
dirtyLevel = updateDirty(dirtyLevel) { dirtyLevel -> dirtyLevel + 23 }
println(dirtyLevel)
```

##### Misc.

The `vararg` modifier allows you to pass in a variable number of arguments to a function or constructor.

```
Vegetables("Carrots", "Beans", "Celery"))

class Vegetables(vararg val toppings: String) : {
```
