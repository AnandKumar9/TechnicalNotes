#### Working with Subscribers

`func receive<S>(subscriber: S)` - Attaches the specified subscriber to this publisher. The only required function in Publisher protocol. <br>

`func subscribe<S>(S)` - The subscriber to attach to this publisher. So adopters of Publisher call this, whereas implementers of Publisher need to implement `receive<S>(subscriber: S)`. <br>

```
subscriber1 = TestNotificationSubscriber()
publisher1?.subscribe(subscriber1!)
```

`func subscribe<S>(S) -> AnyCancellable` - Attaches the specified subject to this publisher and retuens a Cancellable. <br>


#### Connecting Simple Subscribers

`func assign<Root>(to: ReferenceWritableKeyPath<Root, Self.Output>, on: Root) -> AnyCancellable` - Available when Failure is Never. Assigns each element from a publisher to a property on an object. <br>

`func assign(to: inout Published<Self.Output>.Publisher)` - Available when Failure is Never. Republishes elements received from a publisher, by assigning them to a property marked as a publisher. <br>

> Understand the above functon better.

`func sink(receiveCompletion: ((Subscribers.Completion<Self.Failure>) -> Void), receiveValue: ((Self.Output) -> Void)) -> AnyCancellable` - Attaches a subscriber with closure-based behavior. <br>

`func sink(receiveValue: ((Self.Output) -> Void)) -> AnyCancellable` - Attaches a subscriber with closure-based behavior to a publisher that never fails. (This is what I have used in most of the examples.)  <br>

##### The thread where the sink closure runs

By default, the `sink {}` closure runs on the same thread as the one where the event is emitted. `receive(on:)`  whereas causes it to be received in the specified queue.

HolySwift article ([link](https://holyswift.app/swift-and-combine-which-thread-runs-my-sink-closure/))

#### Specifying Schedulers

`func subscribe<S>(on: S, options: S.SchedulerOptions?) -> Publishers.SubscribeOn<Self, S>` - Specifies the scheduler on which to perform subscribe, cancel, and request operations. <br>

In the below example, the `subscribe(on:options:)` operator causes ioPerformingPublisher to receive requests on backgroundQueue, while the `receive(on:options:)` causes uiUpdatingSubscriber to receive elements and completion on RunLoop.main.
```
let ioPerformingPublisher == // Some publisher.
let uiUpdatingSubscriber == // Some subscriber that updates the UI.

ioPerformingPublisher
    .subscribe(on: backgroundQueue)
    .receive(on: RunLoop.main)
    .subscribe(uiUpdatingSubscriber)
```

> In contrast with receive(on:options:), which affects downstream messages, subscribe(on:options:) changes the execution context of upstream messages.

`func receive<S>(on: S, options: S.SchedulerOptions?) -> Publishers.ReceiveOn<Self, S>` - Specifies the scheduler on which to receive elements from the publisher. <br>

In the below example, the `subscribe(on:options:)` operator causes jsonPublisher to receive requests on backgroundQueue, while the `receive(on:options:)` causes labelUpdater to receive elements and completion on RunLoop.main.

```
let jsonPublisher = MyJSONLoaderPublisher() // Some publisher.
let labelUpdater = MyLabelUpdateSubscriber() // Some subscriber that updates the UI.

jsonPublisher
    .subscribe(on: backgroundQueue)
    .receive(on: RunLoop.main)
    .subscribe(labelUpdater)
```

> Prefer receive(on:options:) over explicit use of dispatch queues when performing work in subscribers.

---- Understand these better

#### Encoding and Decoding

`func encode<Coder>(encoder: Coder) -> Publishers.Encode<Self, Coder>` - Specifies the scheduler on which to perform subscribe, cancel, and request operations. <br>

`func decode<Item, Coder>(type: Item.Type, decoder: Coder) -> Publishers.Decode<Self, Item, Coder>` -  <br>


#### Identifying Properties with Key Paths

`func map<T>(KeyPath<Self.Output, T>) -> Publishers.MapKeyPath<Self, T>` -  <br>

`func map<T0, T1>(KeyPath<Self.Output, T0>, KeyPath<Self.Output, T1>) -> Publishers.MapKeyPath2<Self, T0, T1>` -  <br>

`func map<T0, T1, T2>(KeyPath<Self.Output, T0>, KeyPath<Self.Output, T1>, KeyPath<Self.Output, T2>) -> Publishers.MapKeyPath3<Self, T0, T1, T2>` -  <br>

#### Adding Explicit Connectability

`func makeConnectable() -> Publishers.MakeConnectable<Self>` -  <br>
