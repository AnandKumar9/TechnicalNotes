###### Classes and methods

```
UIStackView -> axis
UIStackView -> alignment
UIStackView -> distribution
UIStackView -> arrangedSubviews
UIStackView -> spacing
UIStackView -> baselineRelativeArrangement (Only for vertical stack views, specifies if the vertical spacing between views is measured from the baselines.)
UIStackView -> layoutMarginsRelativeArrangement (Specifies if the stack view lays out its arranged views relative to its layout margins.)

UIStackView -> -initWithArrangedSubviews:
UIStackView -> -insertArrangedSubview:atIndex:
UIStackView -> -addArrangedSubview:
UIStackView -> -removeArrangedSubview:

UILayoutConstraintAxis 
UIStackViewDistribution 
UIStackViewAlignment 
```
