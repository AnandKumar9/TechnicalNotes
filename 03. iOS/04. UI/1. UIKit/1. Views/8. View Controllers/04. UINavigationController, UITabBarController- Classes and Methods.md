[toc]

```
UINavigationController
UINavigationControllerDelegate
UINavigationBar
UINavigationItem
UINavigationBarDelegate
UIBarItem
UIBarButtonItem
```

```
UITabBarController
UITabBarControllerDelegate
UITabBar
UITabBarItem
```

```
UIViewController -> UINavigationController, UITabBarController
UIView -> UINavigationBar, UIToolbar, UITabBar
NSObject -> UINavigationItem
UIBarItem -> UIBarButtonItem, UITabBarItem
```

##### UINavigationController

```
-initWithRootViewController
-pushViewController:animated:
-popViewController:animated:
-popToViewController:animated: (can show the animation transition only for the root view controller)
-popToRootViewControllerAnimated: (can show the animation transition only for the root view controller)
-viewControllers
-topViewController
-visibleViewController
-navigationBar
-toolbar
-setViewControllers:animated: (to replace the entire navigation stack with the passed view controllers, just one animation is performed)
-toolbarHidden:
```

##### UINavigationControllerDelegate

```
-navigationController:willShowViewController:animated:
-navigationController:didShowViewController:animated:
```

##### UINavigationBar

```
-items
-topItem
-backItem
-delegate
```

##### UINavigationItem

```
-backBarButtonItem
-leftBarButtonItem
-rightBarButtonItem
-titleView
-title
-prompt
```

##### UINavigationBarDelegate

```
-navigationBar:shouldPushItem:
-navigationBar:didPushItem:
-navigationBar:shouldPopItem:
-navigationBar:didPopItem:
```

##### UIBarButtonItem

```
-initWithCustomView:
```

##### UITabBarController

```
-tabBar
-viewControllers
-moreViewController
-selectedIndex
-selectedViewController
-setViewControllers:animated:
-customizableViewControllers
```

##### UITabBarControllerDelegate

```
-tabBarController:shouldSelectViewController:
-tabBarController:didSelectViewController:
-tabBarController:willBeginCustomizingViewControllers:
```

##### UITabBar

##### UITabBarItem

```
-initWithTitle:image:tag:
-badgeValue
```

##### UIViewController

```
-isMovingToParentViewController (can be called in viewWillAppear:, viewDidAppear:, can be used to determine if a view controller has loaded due to a push on navigation stack and not a pop)
-isMovingFromParentViewController (can be called in viewWillDisappear:, viewDidDisappear:, can be used to determine if a view controller is disappearing due to a pop on navigation stack and not a push)
-navigationController
-navigationItem (The navigation item used to represent the view controller in the parent's navigation bar)
-toolbarItems
-setToolbarItems:animated:
-tabBarController
-tabBarItem
```



